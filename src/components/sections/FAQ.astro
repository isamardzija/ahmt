---
import BackgroundPattern from "../../assets/chakras.png";
import data from "../../lib/data.ts";
import H2 from "../typography/H2.astro";
import Paragraph from "../typography/Paragraph.astro";
import { FAQCard } from "../FAQCard.tsx";
---

<section class="px-body flex flex-col gap-[50px] py-[250px]">
    <div class="flex flex-col gap-5" id="faq">
        <H2>{data.faqs.heading}</H2>
        <Paragraph>{data.faqs.subheading}</Paragraph>
    </div>
    <ul class="grid gap-4 md:grid-cols-2 xl:grid-cols-3 max-w-7xl mx-auto">
        {data.faqs.list.map((faq, i) => <FAQCard i={i} client:load question={faq.question} answer={faq.answer} />)}
    </ul>
</section>

<style define:vars={{ backgroundPattern: `url("${BackgroundPattern.src}")` }}>
    section {
        background-image: var(--backgroundPattern);
    }
</style>
